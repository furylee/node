const getList = function (author, keyword) {
    return {
        name: "lee",
        id: 1
    }
}

const getDetail = function (id) {
    return {
        name: "detail",
        id
    }
}

module.exports = {
    getList,
    getDetail
}
